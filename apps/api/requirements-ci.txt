# CI installs exactly what production installs.
#
# This file used to duplicate requirements.txt minus the heavy OCR extras, and it drifted:
# tesseract/poppler were apt-installed in CI while pytesseract and pdf2image were missing
# here, so apps/prescriptions/tests/test_ocr.py un-skipped its Tesseract cases (they gate on
# the binaries via shutil.which) and then died on `import pytesseract`.
#
# Now that torch/easyocr live in requirements-easyocr.txt rather than requirements.txt, the
# base file is already light enough for CI, and there is nothing left to duplicate. Keep it
# that way: this indirection cannot drift.
-r requirements.txt
